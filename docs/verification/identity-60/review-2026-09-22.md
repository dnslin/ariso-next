# PR #100 双角度复审

日期：2026-09-22。初审基准 `97ad2ce`，两个独立 agent 只读评审同一 PR。主线程修复后分别回读并复核，两个结论均为 **Approve，无剩余必修项**。

## 发现与修复

| 角度                                                                       | 发现                                                                                             | 修复与证据                                                                                                                      |
| -------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------- |
| `code-review-and-quality`：需求、行为、边界与测试                          | P2：带换行、TAB 或 CR 的畸形 `returnTo` 可在 URL 解析时抛异常，使登录页渲染失败                  | 仅在 URL 解析边界捕获无效输入，采用既有 `/admin` 默认入口；3 个新增参数先复现失败，修复后通过，真实 HTTP 用例验证登录表单可渲染 |
| `code-review-and-quality` 与 `thermo-nuclear-code-quality-review` 独立发现 | P2：后台会话核对和主动退出共用提示与跳转控制；成功核对可清掉退出失败，晚到空会话可覆盖已退出提示 | 拆分 `sessionError` / `signOutError`，各流程只更新自己的错误；退出中忽略后台结果，成功退出后保持忙碌直到离页，仅失败允许重试    |

结构审计覆盖表单、共享字段、输入校验、鉴权与回跳边界、浏览器场景和运行器。没有新增通用状态机、服务层或依赖，没有文件跨过 1,000 行。运行器编排对应实际双宽度、空目录和重启需求，没有要求为拆分而拆分。

两个 agent 都对实际 `SessionControls` 源码做局部确定性执行：原实现确认退出后释放晚到的后台空会话，会先后导航到 `signed-out`、`expired`；最终修复后始终只有 `signed-out`。此项为局部执行证据，不冒充浏览器。

## 实际验证

环境：macOS ARM64，Node 24.19.0、pnpm 11.19.0，已有 Ego Lite / Chrome 152。没有下载浏览器或运行本地 Docker。

| 命令                                                                                                                         | 结果                                                     |
| ---------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------- |
| `pnpm exec vitest run --project unit tests/unit/identity/return-to.test.ts`（修复前）                                        | 3 个新增非法 URL 用例失败，复现 `TypeError: Invalid URL` |
| `pnpm exec vitest run --project unit tests/unit/identity/return-to.test.ts tests/unit/identity/validation.test.ts`（修复后） | 22 项通过                                                |
| `pnpm run lint`、`pnpm run typecheck`、`pnpm run format:check`                                                               | 通过                                                     |
| `pnpm run test:unit`                                                                                                         | 231 项通过                                               |
| `pnpm run build`                                                                                                             | 通过；保留既有可选 SQLite Debug 二进制追踪提示           |
| `pnpm exec vitest run --project integration tests/integration/identity/auth.test.ts`                                         | 最终构建上 17 项通过                                     |
| `node docs/tasks/check.mjs`、`node docs/tasks/check.mjs --self-test`、`git diff --check`                                     | 通过；120 任务、298 需求，5 个拒绝用例通过               |

Ego 第一轮 `EGO_TASK_SPACE=2 EGO_KEEP_SPACE=1 BROWSER_REPORT_DIR=test-results/issue-60-review-browser pnpm run test:browser` 通过，1440/390 两端覆盖退出失败后 focus 保留错误，以及真实响应重排下的退出提示。随后成功退出状态保留至离页的最后补丁已重新构建并经两个 agent 确定性验证。最终浏览器重跑使用同一 TaskSpace 2，但 Ego 返回用户已接管，运行器非零退出并清理临时进程/目录；未标记该次重跑通过；后续由所有者接手手工验证，见下方确认。第一轮[运行报告](./review-browser/runner.json)及 [1440](./review-browser/identity-1440-restart.json) / [390](./review-browser/identity-390-restart.json)会话回归报告保留，最终补丁后的后续验证状态在 PR 更新。

## 验收与 CI 调整

全部 65 个 GitHub Issue 已检查并回读，含相关条款的 #57、#60、#76、#77、#79、#81 已移除设备实测要求，其余正文无相关条款。任务卡和设计验收入口同步到[共用规则](../../tasks/execution.md#前端共用验收)，历史证据和冻结 PRD 未改写。

CI 与 Docker 工作流使用 GitHub 原生路径过滤跳过纯文档 PR/主分支推送，并取消同一 PR 已被新提交替代的旧检查。手动运行、Release 及发布条件不变。main 分支保护 API 返回未保护，分支 rules 为空；当前不存在被路径过滤留下 Pending 的必需检查。路径过滤按整个 PR 差异判断，因此代码 PR 追加文档仍会运行；本轮收齐修改一次推送，远端结果只回填 PR，避免再追加结果提交。

配置依据：[GitHub 工作流语法](https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax)。本地格式检查解析 YAML；代码 PR 实际执行由最终 PR 检查验证。未单独制造纯文档 PR 或连续推送来触发跳过/取消场景，这些不标记为实测通过。

## 手工界面反馈修正

所有者手工检查发现输入焦点叠框、文字显隐按钮和过大的按钮圆角。根因是未分层的全局焦点规则覆盖了 HeroUI 输入组内部样式；现将全局默认焦点和标题放入 base 层，让 HeroUI 的组件焦点样式及 Tailwind 工具类生效。认证表单继续使用 HeroUI，布局与间距迁入 Tailwind，删除 `identity.css`；操作按钮圆角为 12px，密码显隐使用 `lucide-react@1.47.0` 的 Eye/EyeOff，保留可访问名称、键盘操作和 Tooltip。登录字段图标同时统一到 Lucide，删除不再引用的两份 SVG。

本轮实际执行 lint、typecheck、format:check 和 test:unit（231 项），均通过；运行中 dev 的 `/setup` HTTP 200，服务端 HTML 包含两个 Lucide 显隐图标和 12px 圆角类。新增 Ego 回归检查键盘显隐、内部无重复描边、外层焦点环与按钮圆角，尚未执行，不沿用此前截图作为本次视觉通过证据。保持 dev 环境供所有者手工验证，本轮不重新运行生产构建，也不因每条界面反馈单独推送触发 CI。

## 所有者手工验收确认

2026-09-22，所有者在本地 dev 环境检查修正后的界面，确认“现在没什么问题了”，要求更新 PR。本轮界面反馈已获手工验收，不再等待浏览器接管或已取消的设备实测。新增自动化视觉断言仍记录为未执行，不将手工确认改写为自动化通过。

更新 PR 前再次执行 `pnpm install --frozen-lockfile`、`pnpm run lint`、`pnpm run typecheck`、`pnpm run format:check`、`pnpm run test:unit`、`node docs/tasks/check.mjs` 和 `git diff --check`；全部通过，单元测试 231 项通过。两个原评审 agent 分别回读本轮 UI 与规则增量，正确性和结构复核均为 Approve、无必修项。生产构建、完整集成及 AMD64/ARM64 容器验证由此次统一推送触发的 Actions 执行，结果只回填 PR，避免额外提交重跑。
