# PR #120 三项评审建议优化

基于 `8264dc7`，仅落实已确认的三项可选建议。继续更新 PR #120，需求编号与任务定义不变。

## 计划与验收

- [x] 任务步骤文案：在图库展示层将 identify、各图片版本步骤与 complete 显示为中文；后端记录不变。浏览器验证真实活动/失败任务的中文文案。
- [x] 页面鉴权：identity 提供服务端页面入口函数，admin/library 复用。未登录保留 returnTo，有会话 Cookie 的失效请求保留 reason=expired，非鉴权异常原样抛出。补充针对性测试，并验证两条真实页面调用路径。
- [x] 图片路径：delivery 提供相对路径构造，公开 URL 与图库调用同一实现。验证特殊字符编码、默认版本、显式版本、下载参数，以及公开域名变化不改变图库同源路径。
- [x] 完成安装、格式、lint、类型、单元、构建、集成与 Ego 浏览器检查；补充证据、审计、提交并更新 PR。

## 边界与风险

不引入新依赖、表结构或通用状态框架。鉴权只抽取页面入口，不改变 API 权限校验。相对路径必须保留浏览器当前来源，不能让公开域名配置改变管理 Cookie 的发送目的地。错误保留诊断信息。

各项先补测试再完成对应修改。delivery 路径工作可独立开展；共享构建与浏览器验证由主 agent 串行运行。最终结果记录在同目录 README，清单只记录完成状态，不复制验证报告。
