# 回收站管理预览独立代码审计

2026-09-26 初审，2026-09-27 修复复审。使用 `code-review-and-quality`，审计相对 `46302d9` 的本轮修改。依据为用户已批准的登录管理员列表缩略图和详情预览、现行 handoff 与 library / delivery / media 规则；授权不扩展公开外链、下载、复制链接或主题功能。

## 当前结论

本轮代码审计无剩余阻断项。初审发现的缩略图失败恢复问题已修复，真实浏览器回归已通过。已复核完整浏览器运行器及两端 M2 报告为 `passed`，集成日志为 62 个文件、512 项通过。此结论不代替独立设计评审或用户人工验收。

## 发现与修复

- **已修复且实际回归通过：缩略图读取失败后列表刷新不能重试。** 初版 `failed` 状态在请求失败后保持为 true，父 key 仅使用稳定 URL。当前 key 加入 `list.dataUpdatedAt`，成功刷新列表后会重新挂载图片组件并重试相同 URL，没有清空错误来伪造加载成功。
- 新增浏览器回归通过 CDP 阻断真实管理预览请求，要求页面出现“预览读取失败”，在 `finally` 解除阻断后点击实际“刷新回收站”，再要求图片完成解码且 `naturalWidth > 0`。该检查能够捕获原问题，没有用模拟成功响应替代图片请求。
- 详情没有单独的成功状态刷新按钮；记录读取错误时组件已卸载，重试 metadata 后会重新挂载预览。浏览器刷新或返回列表后重新进入也会重置图片失败状态，因此当前列表修复不需要改变详情读取流程。

## 已核对的边界

- 管理预览路由要求显式版本，拒绝下载标志与其他参数，使用真实所有者会话；Bearer Token 与分享 Cookie 不替代管理员身份。
- `trash-preview` 选择规则要求当前处于回收站、没有删除状态、存储启用、实际版本已保存。已恢复、deleting、cleanup_failed、停用及缺失版本均不能读取。
- 文件打开的异步间隔后沿现有机制重新读取会话和资产状态。会话过期或图片恢复后返回拒绝，并关闭已打开流。
- 原 `/i` 入口保留默认 `published` 规则，对回收图片仍拒绝所有者和匿名访问；新的 DTO 仅提供管理预览路径，复制和下载字段继续为空。
- 管理预览复用 no-store、nosniff、实际流传输与条件请求处理。只有成功所有者能取得内容，且路由未挂接统计消费者；现有统计条件也排除所有者。
- 缩略图查询仅查询当页最多 40 个 ID 的已存 thumbnail，未增加逐行查询或创建派生版本。详情复用现有预览组件，不引入另一套解码/流生命周期。

## 测试审查与证据边界

已审查新增的 SQLite/真实 HTTP 集成测试：覆盖四版本读取、匿名及替代凭据拒绝、HEAD/no-store、删除/停用/恢复状态、条件请求之前的授权、文件打开时身份失效及流关闭。真实 HTTP 测试在进程停止后核对三张统计表为空。浏览器脚本新增真实缩略图/详情解码和匿名拒绝断言，并保留 `/i` 全版本回收拒绝与恢复原链接验证。

审计者本轮执行文件读取、`git diff`、`git status` 和 `rg`，没有独立运行构建、测试或浏览器。实际读取了本轮最终运行输出： [图库与错误恢复](./trash-preview/browser/library.json)、[桌面 M2](./trash-preview/browser/m2-1440.json)、[手机 M2](./trash-preview/browser/m2-390.json)、[完整运行器](./trash-preview/browser/runner.json)。图库报告包含真实预览请求阻断后刷新解码成功；两端 M2 包含管理预览、匿名拒绝、原外链回收拒绝、恢复与重启后统计核对。完整运行器于 `2026-09-26T16:12:57.793Z` 完成（北京时间 2026-09-27），`status=passed` 且临时目录已删除。单元日志为 30 文件、461 项通过；集成日志为 62 文件、512 项通过。构建、类型与 lint 由实现者执行并报告退出码 0，审计者读取了对应日志但未重复运行。设计对照及用户人工验收分别由对应记录承接，不能由本代码审计代替。

## 清理失败文案补审

2026-09-27 再次核对 `detail.ts` 与对应集成断言：`cleanup_failed` 现在准确显示“图片清理失败，无法预览”，`deleting` 仍显示“图片正在删除”。两种状态仍不给出预览路径，未改变访问、缓存或统计规则；无新增阻断项。

已读取 [文案浏览器复验](./trash-preview/browser/cleanup-label-recheck.json)，状态为 `passed`，涵盖两端、两种主题下的准确提示和无图片内容，恢复测试状态后相同缩略图仍可解码。实现者另报告先取得旧文案断言失败，再取得相关 6 项通过，并重新构建。此前 512 项集成及完整浏览器结果属于同轮文案修正之前；本次只重跑受影响检查，未将它们写成文案修正后重新完整执行。
