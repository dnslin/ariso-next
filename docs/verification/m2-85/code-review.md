# Issue #85 独立代码审计

2026-09-26。独立审计 agent 使用 `code-review-and-quality`，只审查代码及已有证据，没有修改业务实现或代替实现者运行验证。

## 结论

最终代码审计无阻断项。已复核第三轮完整浏览器报告及桌面、手机 M2 两阶段结果，均为 `passed`。此结论不代表设计验收或用户人工验收通过。

## 范围与核对结果

已读取项目规则、执行约定、Issue #85、任务卡、media / upload / delivery / analytics 契约、相关处理与恢复实现，以及现有身份、上传、图库和回收测试。

- 真实文件经页面选择与上传，详情复制使用实际 Clipboard API，下载等待真实浏览器下载事件；磁盘和下载文件与原始样本逐字节比较。
- 两格式、两可见性的回收与恢复均通过页面操作；检查原 ID、原 URL 和各版本权限，没有直接改数据库制造成功结果。
- 失败夹具在真实 worker 最终结算时触发数据库错误；已有版本与失败任务均须存在，匿名读取须拒绝。调度持有夹具仅用于重启恢复，不冒充媒体工具运行中崩溃。
- 三张统计表在周期刷库后精确核对，并保存状态供真实 stop/start 后再次核对。所有者、私有、失败、回收、HEAD 和缩略图不增加计数。
- 一次性触发器使用 `finally` 删除，匿名页面使用 `finally` 关闭；测试数据使用运行器的独立临时目录。
- 旧测试只修改并还原默认链接版本；新用例要求的压缩、水印和存储设置未被改变。缺少真实默认值会使断言失败，不静默重置设置。

## 复审修正

- 已补充保存 `report.core` 并在 after 阶段重新核对统计；修复最初只有重启前核对的证据缺口。
- 报告改为 `actual stop/start`，不把周期刷库结果称为关停刷库专项验证。
- 派生格式断言改为准确的 `WEBP`，与现有处理器保存值一致；未放宽为忽略大小写或删除断言。
- 登录仅在真实页面出现 HTTP 429 时等待现有提交按钮倒计时结束，再尝试一次。未清空限流数据、修改产品超时或吞掉其他错误；后续上传页面仍必须出现。
- M2 截图明确设置浅色，并记录视口宽高。身份页面只新增设计尺寸截图，原断点、主题及行为检查保留。

## 已有证据与未执行项

早期专项 before 的 `running` 记录仅作调试依据，未计作完整通过。最终复核实际读取第三轮运行输出，归档入口为 [桌面 M2](./browser/m2-1440.json)、[手机 M2](./browser/m2-390.json) 和 [运行器报告](./browser/runner.json)。两端报告均为 `passed`，分别记录 1440×1080、390×844 浅色视口，包含四种格式/权限组合、处理失败、重启后三表精确核对以及同任务、快照、原图对象和单份版本恢复。

同轮身份 setup/restart、图库、上传及轮询报告均为 `passed`。运行器于 `2026-09-26T13:11:21.018Z` 完成，整体状态为 `passed`，`temporaryDirectoryRemoved=true`。审计同时回读最终脚本与差异，没有发现跳过失败、删除断言或伪造成功状态。

补充双浏览器记录见 [second-browser/result.json](./second-browser/result.json)。它明确区分 Ego Lite 所有者与同 origin 的 Chrome 154 匿名身份，记录公开读取、私有拒绝、回收拒绝、恢复及持久计数；两个 Ego 页面没有被当成两个浏览器。此审计读取了记录，未重新执行该人工操作；截图的设计对照由独立设计评审负责。

本审计实际执行了文件读取、`git diff`、`git status`、`rg` 与 `gh issue view 85 --json title,body,comments,state,url`。未独立执行安装、构建、单元、集成或浏览器测试，其结果须以实施报告和实际运行日志为准。本次已完成最终浏览器报告复核。设计缺口和用户人工验收不得由本代码审计结论替代。
