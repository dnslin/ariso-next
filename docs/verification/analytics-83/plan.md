# PR #121 浏览器回归修复计划

2026-09-26。任务仍归属 [Issue #83 / PR #121](https://github.com/dnslin/ariso-next/pull/121) 的交付跟进；用户已授权修复上轮暴露的登录回归问题。保留统计模块与 #84 的边界，不执行 Docker、发布或部署。

1. **定位两个失败。** 阅读 LoginForm、会话控制、浏览器场景及失败报告，复现错误提示焦点与重启后等待邮箱输入框的失败，区分生产行为与场景导航竞态。验收：明确每个问题的触发顺序与责任文件，保留修复前证据。
2. **最小修复和回归。** 只修改确认有问题的登录焦点代码/浏览器场景。验收：错误提示出现后获得焦点；连续失败及真实登录退出正常；场景等待真实导航完成，不用固定延时或放宽断言。
3. **本地验证、审计和交付。** Node 24 冻结安装、lint、格式、类型、相关测试、生产构建及现有 Ego Lite 完整浏览器回归。验收：检查全部通过，独立代码审计通过，证据与 PR 描述更新，推送后转待评审；保留历史失败报告，不合并或关闭 Issue。

步骤 2 依赖步骤 1，步骤 3 依赖步骤 2。修复结果和执行状态集中记录于同目录 README，避免另外维护任务规则。
