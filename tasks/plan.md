# PR #124 读取与回收核对竞态修复计划

范围：两份评审指向同一请求生命周期问题。普通详情读取失败不应卸载仍负责写后核对的动作。继续使用现有 media 接口、TanStack Query 和核对流程，不新增依赖或状态机。

顺序：先通过实际界面复现旧 GET 失败中断回收，再修复请求归属，最后验证及复审并更新原 PR。任务见 [todo.md](./todo.md)。

设计：开始写入时取消精确图片详情查询；提交与未知结果期间暂停普通读取，由核对流程负责状态。保留主动离页忽略迟到回调。检查图库和回收站两处调用。

验收：旧 GET 失败不能丢失动作结果；写响应丢失后仍能手动核对且不重复写；不破坏恢复、401/404、离页与焦点行为。

验证：Node 24 / pnpm，先红后绿的 Ego 浏览器回归，相关单元/集成、格式、lint、类型和构建；两个评审角度复核。远端按项目 execution.md 回读，不触发发布。
