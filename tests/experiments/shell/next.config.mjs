// Webpack resolves framework entrypoints from this fixture. Turbopack with a
// shared repository root also discovers the production instrumentation hook.
const config = { agentRules: false };

export default config;
